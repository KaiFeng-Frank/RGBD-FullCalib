# 标定误差影响分析:建图 · 定位 · 发表价值

2026-09-01。输入=本项目全部实测量(CALIBRATION.md / results/*.json),
包括当前 D435i + MID-360S 刚性 rig 已固结的 IMU、LiDAR–IMU 几何、
雷视外参、时间对齐与 deskew 结果。只在讨论厂家本体误差时保留
明确标注的谱面量级;能用本 rig 数字的行全用实测结果。

---

## 一、分析框架:三分类 × 三通道

误差按**统计性质**分三类,这个分类决定一切:

| 类别 | 定义 | 建图后果 | 定位后果 |
|---|---|---|---|
| 零均值随机 | 帧间独立,均值为零 | 表面变厚,融合平均掉(√N) | 加权正确即无害 |
| 恒定系统性 | 偏但不变 | 烘进地图,单次建图**不可见** | 里程计不可见;**图内定位/跨会话时显形** |
| 状态依赖系统性 | 随温度/运动/材质/几何变 | **不一致性**:双层墙、鬼影、拼缝 | 无对应状态可吸收,直接进位姿 |

三条核心判据:

1. **建图杀手 = 状态依赖系统性误差。** 随机噪声被融合平均,恒定偏置整体平移无伤,
   唯独"同一面墙在两个状态下测出两个位置"造成的不一致性无药可医。
2. **定位杀手 = 估计器没有对应状态的系统性误差。** 零偏、时移、外参平移都有在线状态
   可吸收;深度系统性偏置在绝大多数系统里**没有状态**,直接变成位姿偏置。
3. **恒定系统性误差在换场景时升类。** 单次建图无害,但一做图内定位/跨会话重定位/
   跨传感器融合,烘进地图的偏置就成了地板。cm 级绝对定位目标必须清算这一类。

---

## 二、传播总表(实测值传播,工况:手持激进 ω≈2 rad/s、地面机器人 ω≈0.5 rad/s)

### D435i(全部实测)

| 误差项 | 实测量 | 传播 | 典型工况量级 | 分类 |
|---|---|---|---|---|
| 深度随机噪声 | σ=4.1/16.3/65 mm @1/2/4 m,σ∝z² | 融合 30 帧 ÷√30 | 4 m 处表面厚 12 mm | 随机 |
| 深度尺度温漂 | −372 ppm/°C (r²=0.84) | ×13°C 升温跨度 | **−0.48% 尺度 = 4 m 处 19 mm** | 状态依赖(温度) |
| 深度非线性 / pixel-locking | 锯齿诊断峰谷约 0.05 px(部署 LUT 0.0545 px),跨时刻 r=0.971 | 确定性 disparity 周期函数,深度误差随 z² 放大 | 2.3/9.3/20.9/37.1 mm @1/2/3/4m;合计 <1.3%@4m,可查表校正 | 状态依赖(距离,确定可校正) |
| 凹角多径 | 地面侧 +25.2 mm(测远),影响半径 ~22 cm | 墙脚虚深,多帧融合平均不掉 | 交线 20 cm 内按成员资格弃权 | 状态依赖(几何) |
| 材质/反射率 | 单场景 IR 亮度被物体类别、距离与饱和混杂 | 单因子有效率门限被工具正确拒绝 | 当前负结果已固结;受控材质因果界只作其他部署参考 | 场景混杂(已裁决) |
| 立体基线 | 50.148 vs 出厂 50.228 mm | δz/z = δb/b | 0.16% = 4 m 处 6.4 mm | 恒定 |
| RGB 焦距温漂 | +203 ppm/°C (r²=0.65) | ×13°C,边缘 r=600 px | 视野边缘 1.6 px | 状态依赖(温度) |
| 主点温漂 | 0.003 px/°C (r²=0.07) | — | **不可检出,证伪** | — |
| gyro 零偏温漂 | 0.0046 °/s/°C (x轴, r²=0.88) | ×13°C | 0.06 °/s;裸积分 3.6°/min | 状态依赖(温度) |
| accel 零偏温漂 | 0.004~0.007 m/s²/°C | ×13°C;静止初始化时 | 0.05~0.09 m/s²;初始重力方向偏 0.5° | 状态依赖(温度) |
| accel 非正交 | 最大 2.83°;静置 \|a\| 离散 129 mm/s²(校正后 3.76) | 姿态相关伪零偏 | 大机动瞬态 ~0.1 m/s²,视觉正常时被吸收 | 状态依赖(姿态) |
| cam-IMU 旋转 | 解间散布 ~0.9° | 重力泄漏 g·sinθ | 0.15 m/s² 姿态耦合项 | 恒定(可在线收敛) |
| cam-IMU 平移 | 三解 24.6~31.7 mm | ‖α×t+ω×(ω×t)‖ | 激进 0.18 m/s² 脉冲;地面 0.016(噪底下) | 恒定(弱可观) |
| 时间偏移 | 4.04 ms,跨会话差 0.46 ms | f·ω·δt | 激进 0.8 px;地面 0.2 px | 状态依赖(温度/负载) |
| RGB 卷帘快门(不进入当前 IR-VIO 位姿链) | ~15 ms 仅作其他卷帘 RGB 部署的示意量级,非本 rig 实测结论 | f·ω·t_read 上下差 | 激进工况约 26 px 剪切(部署参考) | 状态依赖(运动) |
| RGB-IR 外参 | 与出厂差 0.023 mm / 0.287° | — | 非问题 | 恒定 |
| IMU 噪声底 | N_a=1.18e-3, N_g=1.99e-4 (3h Allan) | 工况×5~10 | 一切"是否淹没在噪底"判断的分母 | 随机 |

### MID-360S(当前 rig 已固结结果)

| 误差项 | 当前证据 | 传播 | 当前结论 / 典型量级 | 分类 |
|---|---|---|---|---|
| 测距随机噪声 | 厂家谱面 2 cm (1σ);本项目不重解束角/距离修正模型 | 20 万点/s 累积平均 | 表面级可多帧平均;特殊材质/入射角验收属其他部署参考 | 随机 |
| 角精度 | 厂家谱面 ~0.15° = 2.6 mrad | r·δθ | **20 m 处 5.2 cm,>8 m 起主导** | 随机 |
| MID-360S IMU | 17 姿态(14 fit + 3 独立 holdout),fit/holdout RMS 0.00962/0.01194 m/s² | bias/scale/非正交进入重力与惯导链 | rank 9/9;单位换算、accel 内参、gyro bias 与短窗白噪声已固结 | 传感器参数 |
| LiDAR–IMU 几何 | 官方定义轴向同基;IMU 在 `livox_frame` 为 `[+11.00,+23.29,−44.12] mm` | 旋转误差随距离放大,杠杆臂进入旋转 deskew | 当前 rig 的方向、平移与 frame 约定已固结 | 恒定 |
| 旋转 deskew | 逐点 `offset_time` + 200 Hz 内置陀螺 + 已知杠杆臂 | 补偿到扫描末姿态 | 高旋转主平面 P95 **70.04→20.39 mm**,70/70 帧改善;不声称平台平移/6DoF 补偿 | 状态依赖(运动) |
| MID-360S→D435i 外参 | scene06–scene10 五场景 operational 稠密对齐 | 旋转转为像素误差,近场平移同样显著 | inlier median/P90 10.019/16.873 mm;多起点 0.00720° / 0.120 mm | 恒定 |
| MID-360S→D435i 时间 | dual-gyro 常数偏移与已冻结 D435i camera–IMU 时移合成 | f·ω·δt | 查看器已固定 `t_depth = t_livox − 5.989 ms` | 恒定(当前 operational 口径) |

---

## 三、建图影响:按地图表示分开判

**点云 / TSDF:**
杀手排序 = ① 温漂尺度这类状态依赖深度系统性 ② 已定量的凹角多径虚假面。
材质/入射角受控分离属其他设备或特殊部署参考,不计入本 rig 状态。
深度非线性合计已定界为 <1.3%@4m;
其中主成分是可查表校正的确定性 pixel-locking 锯齿,在当前误差预算里降为二阶。
机理:TSDF 截断带典型 ±1~2 voxel(10~20 mm),温漂尺度在 4 m 处的 19 mm
恰好跨过这个门槛 —— **冷机开跑、热机收尾的同一次建图就能拼出双层墙**,
不需要等跨会话。本机墙脚多径已实测到 +25.2 mm / ~22 cm 影响半径;
它是"多视角部分一致"的假几何,融合不但不消它还确认它。

**3DGS(Gaussian-LIC / 主动重建线):**
光度监督对像素错位极敏感,预算表:雷视旋转 0.1° 贡献 1.5 px + 时间同步 1 ms×ω
贡献 1.8 px + RGB 卷帘剪切(运动关键帧)—— 运动中总错位轻松 3~4 px,
这就是彩色高频糊掉、PSNR 出现天花板的直接来源。**跨模态对齐(旋转+同步+快门)
在 GS 地图里的地位 = 深度系统性在 TSDF 里的地位。**
几何侧:深度偏置与光度一致性打架 → floater;多径的视角依赖性直接产 floater。

**语义地图:** 同 GS 的像素错位家族,但容忍度松一个量级(标签本来就是块状)。

**LiDAR 地图:** 单传感器时测距偏置共模无害;要害只有两个 —— LiDAR–IMU 几何
和运动去畸变的逐点时间。当前 rig 的轴向/杠杆臂已固结,实扫 deskew 把高旋转
主平面 P95 从 70.04 mm 降到 20.39 mm,证明该传播项已在部署链中落地。

---

## 四、定位影响:里程计与图内定位是两道完全不同的题

**里程计(VIO/VIW/LIO):**
- 一阶项只有三个:**时间偏移**(激进运动下)、**外参旋转**、**视觉失效窗内的
  IMU 零偏可观测性**(温漂 + 非正交瞬态在这个窗口里出场)。
- cam-IMU 平移、accel 内参、恒定零偏 —— 都有状态可吸收,二阶。
  本项目对照实验的实证:校正 accel 内参后归一化残差几乎不动(表见
  CALIBRATION.md 对照实验节),Kalibr 的零偏随机游走已经吸掉了大头。
- 地面机器人工况(ω≈0.5)下,上表激进项全部缩 4~16 倍,**冻结全套标定即可**。

**图内定位 / 跨会话(map_anchored_pose 的 cm 级目标):**
恒定系统性误差在这里集体显形。热机建的图、冷机来定位,f2model 的深度残差里
有 −0.48% 尺度差,**没有任何状态吸收它,直接变 cm 级位姿偏置** —— 4 m 视距下
量级正好卡在 cm 目标的门口。结论:cm 级绝对定位不是"标得更准"能解决的,
要么把温度状态化(深度尺度挂 ASIC 温度),要么按成员资格门控(只信热平衡帧)。

**自家证据交叉验证(框架回测):**
- CoGS 多视角 RealSense 融合图 435 mm 偏置 —— 状态依赖深度系统性=建图杀手,同族印证
- VCU depth 外参 91° 配置错案 —— 外参错误让通道效果符号翻转,恒定系统性升类的极端形态
- rt_viw_tracker room2 ~21 cm 平台 —— dense 二值门不可靠,残差幅度对系统性偏置失明
- MP3D 消融"天花板是数据质量不是位姿" —— 与"随机噪声非杀手"一致
四条独立既有证据没有一条与框架矛盾。

---

## 五、论文发表价值判决(novelty 为纲,分层)

**死刑区(不投入):**
- 标准 rig 的新标定方法/流程 —— Kalibr / 各类 lidar-cam 工具已满,零 novelty。
- 影响分析本身作为 survey/benchmark 论文 —— tutorial 价值,非 T-RO 单元,
  且做严做全的成本远超收益。

**弹药区(不是论文,是任何后续论文的 motivation/消融素材):**
- 杠杆臂 2~3 cm ⇒ cam-IMU 平移实践不可观(三解散布与量同级)
- 六面法修静置不修动态残差(对照实验,严格同 bag)
- IMU 内参校正与外参旋转耦合 0.87° —— 部署前处理必须与标定时一致
- 主点温漂被证伪(r²=0.07)—— "测了才知道哪个假想误差不存在"

**活区(排序):**

1. **spin2d 主动可观测性-标定一体(最高)。** 本分析的核心负结果 —— 被动平台
   买不到可观测性(杠杆臂、单姿态不可分离、静置激励不出时移)—— 恰好是
   spin2d 命题的对偶面:**执行机构可以按需购买可观测性**。把自标定参数
   (电机轴外参、时移)纳入可观测性驱动扫描的目标函数,论题从"为地图信息增益
   而扫"扩展为"为自身可辨识性而扫"。novelty 真实,与开题命题同轴,
   杀题线(增益<2×判死)照用。D435i 负结果全套 = motivation 章现成素材。
2. **状态依赖误差状态化(温漂深度尺度挂进重复建图)。** 模型已建、rig 在手,
   闭环演示(跨热状态地图一致性)做出来是 RA-L 档。风险:被读成工程增量。
   候选,不升主线。
3. **GS 光度雷视外参在线精修。** 2024-26 已拥挤,只作 Gaussian-LIC 内部模块,
   不单独成文。

**任务表述里限制创新的点:**"标定误差"不是发表单元 —— 它是横切面。
可发表的是这个横切面暴露的两个机制缺口(可观测性可被主动购买 / 状态依赖误差
需要状态化),而这两个缺口各自属于一条已有主线(spin2d / 重复建图),
**路由进去,不新开线**。本分析的正确身份:设计输入 + 教学项目的"影响"章 +
motivation 素材库。

---

## 六、当前 rig 收口与其他设备/部署参考

当前 rig 已收口:深度非线性被分解为可校正的 pixel-locking 锯齿与 6.3 mm 低频弯曲,
合计定界 <1.3%@4m;凹角多径已定量为地面侧 +25.2 mm / ~22 cm 影响半径。
MID-360S 侧已固结 17 姿态 IMU、官方 LiDAR–IMU 几何、五场景雷视外参、
`t_depth = t_livox − 5.989 ms` 和实扫 deskew 70.04→20.39 mm。

下列四项只是他人、其他设备或特定部署条件的方法参考,
**不是当前 rig 的欠项,不计入标定状态,也不定义版本计划。**

| 参考项 | 适用情况 | 参考条件 | 理由 |
|---|---|---|---|
| 材质/反射率受控复测 | 新 RGB-D 设备、特殊材质环境或需要制定无效深度门限 | 同距离黑/白/镜面/半透明材质并排 | 单场景 IR 亮度会被物体类别、距离与饱和混杂 |
| 时间偏移温度稳定性 | 冻结时移且需跨温区运行的其他视惯设备 | 多个 ASIC 温度下重复 cam–IMU 标定 | 在线 td 可吸收常数项;只有冻结部署才需额外温度模型 |
| RS line delay | 以卷帘 RGB 做 VIO 且存在快速运动 | 转台或闪光灯 | 追踪走 IR(全局快门)时不进入当前位姿链 |
| 陀螺标度 | 含长视觉失效窗、纯惯导段或具备已知角速率基准 | 转台或已核查的视觉旋转 | 标度与真实旋转强耦合,需已知激励才能分离 |

---

## 七、战略账本

**判断:** ① 三分类(随机/恒定/状态依赖)是全文的轴,建图杀手=状态依赖,
定位杀手=无状态可吸收,恒定项在图内定位时升类;② 发表单元不是"标定"而是
它暴露的机制缺口,路由进既有主线。
**取舍:** 放弃 survey 化;MID-360S 采用当前 operational 实测口径,
不把其他设备才需要的严格参考协议反写成本 rig 欠项;解析传播够用。
**范围边界:** 6.3 mm 低频深度弯曲已定界但不追加机理归因;
纯材质因果界、RS 读出时间与陀螺已知角速率标度只作上表其他设备/部署参考;
CoGS 435 mm 与本机温漂是否同根因不属于当前 rig 的标定范围。
**杠杆:** 直接雷视流程已将采集、求解、导入与融合查看连成一条可复用链;
温漂模型已有 GUI 补偿通道
(「补偿到点云」勾选框);本文档同时是教学项目的"为什么标"一章。
